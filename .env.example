# Steinway P100 Configuration
STEINWAY_HOST=p100.local
STEINWAY_PORT=84

# Optional: Serial connection (if using RS232 instead of network)
# STEINWAY_SERIAL_PORT=/dev/ttyUSB0
# STEINWAY_SERIAL_BAUDRATE=115200

# Optional: Default zone (MAIN or ZONE2)
STEINWAY_DEFAULT_ZONE=MAIN

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
STEINWAY_LOG_LEVEL=INFO